# add-employee skill

Provide the employee JSON only. Use this JSON shape (field names match the Employee entity):
{
  "fisrtName": "{{fisrtName}}",
  "lastName": "{{lastName}}",
  "department": "{{department}}",
  "phoneNumber": "{{phoneNumber}}",
  "address": "{{address}}"
}

Return only the JSON object (no surrounding text).  Replace placeholders with real values.
